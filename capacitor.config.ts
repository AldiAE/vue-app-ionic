import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'vue-app-ionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
